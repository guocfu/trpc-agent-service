# Copy this file outside source control, replace every value, then apply it.
apiVersion: v1
kind: Secret
metadata:
  name: trpc-agent-secrets
  namespace: trpc-agent
type: Opaque
stringData:
  TRPC_DATABASE_URL: REPLACE_WITH_POSTGRES_DSN
  TRPC_INTERNAL_TOKEN: REPLACE_WITH_INTERNAL_TOKEN
  TRPC_ADMIN_TOKEN: REPLACE_WITH_ADMIN_TOKEN
  TRPC_MODEL_API_KEY: REPLACE_WITH_MODEL_API_KEY
  TRPC_S3_ACCESS_KEY: REPLACE_WITH_S3_ACCESS_KEY
  TRPC_S3_SECRET_KEY: REPLACE_WITH_S3_SECRET_KEY
  POSTGRES_USER: REPLACE_WITH_POSTGRES_USER
  POSTGRES_PASSWORD: REPLACE_WITH_POSTGRES_PASSWORD
  POSTGRES_DB: REPLACE_WITH_POSTGRES_DATABASE
